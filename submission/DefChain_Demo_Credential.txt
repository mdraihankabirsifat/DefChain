DefChain — BCOLBD 2026 Local Prototype Demo

Demo URL: http://localhost:5173
Health URL: http://localhost:5173/api/v1/health

Prerequisites
- WSL2 Ubuntu
- Docker Desktop with Ubuntu WSL integration
- Native WSL Node.js 20 or 22 and npm 10+
- Git, curl, jq, and OpenSSL

Shortest reliable clean full-demo start (run from the repository in WSL):
FABRIC_NETWORK_MODE=full npm run reset && FABRIC_NETWORK_MODE=full docker compose -f docker-compose.app.yml --profile full up -d

Synthetic demo accounts
- Police investigator: police.investigator
- RAB provider officer: rab.officer
- BGB provider officer: bgb.officer
- Customs provider officer: customs.officer
- Independent auditor: auditor

Use the one-click actor presets on the local login page. Local fixture credentials and control-check users are documented in Readme.md; this handoff intentionally contains no generated secrets or session tokens.

Shortest judge sequence
1. Confirm the health URL reports Full Demo, Fabric available, and RAB/BGB/Customs providers.
2. Select Police investigator and open Discovery.
3. Submit the displayed synthetic identifier for the active synthetic case.
4. Open Disclosure and commit the two default scopes to RAB.
5. Switch to the RAB provider, select one requested scope, and commit PARTIAL.
6. Switch back to Police, receive the disclosure, and confirm encryption, signature, and receipt verification.
7. Open Audit timeline and verify all five ledger stages.

Stop all DefChain application and Fabric containers:
npm run stop

All data, actors, identifiers, cases, and organizations in this demo are synthetic or simulated. MATCH is a provider attestation, not proof of guilt, identity correctness, source accuracy, or disclosure entitlement. This is not a production deployment or a claim of government partnership.
