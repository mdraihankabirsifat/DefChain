server {
  listen 80;
  server_name _;

  location /api/ {
    proxy_pass http://gateway-api:4000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Request-ID $request_id;
    proxy_intercept_errors off;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
